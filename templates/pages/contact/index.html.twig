{% extends 'base.html.twig' %}

{% block title %}SymRecupe - Contact{% endblock %}

{% block body %}
<div class="container">
    {% for message in app.flashes('success') %}
   <div class="alert alert-success">
   {{message}}
   </div>
   {% endfor %}
   
    <h1>Formulaire de contact</h1>
    {{form(form)}}
</div>

{% endblock %}
